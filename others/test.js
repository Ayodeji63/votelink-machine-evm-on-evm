let arr = [1, 2, 3];

function name(arr) {
    return arr.foreach((item) => item * 2);
}






const solution = (arr) => {
    // TODO: Implement me!
    return arr.map((el) => el * 2);
}

const _solution = solution;
export { _solution as solution };


arr.sort((a, b) => a - b);